# 📋 تقرير التدقيق الشامل — المرحلة الأولى

**النطاق:** 33 قالباً، 819 سطر CSS، ملف JS واحد، ملف Service Worker، manifest.json، requirements.txt

---

## 🔴 حرج (5)

| # | المشكلة | الملف | التفاصيل |
|---|---------|-------|----------|
| 1 | **manifest.json يشير إلى ملفات `.png` غير موجودة** | `manifest.json:13-14` | الأيقونات مسجلة كـ `icon-192.png` و `icon-512.png`، لكن الملفات الفعلية هي `icon-192.svg` و `icon-512.svg`. فشل تثبيت PWA، وعدم ظهور أيقونة على الشاشة الرئيسية للجوال. |
| 2 | **لا توجد رؤوس CSP (سياسة أمان المحتوى)** | `app.py` (جميع المسارات) | أي سكريبت يمكن حقنه. جميع سكريبتات CDN (Chart.js، Bootstrap، Bootstrap Icons، Google Fonts) غير محمية. اختراق CDN واحد = XSS في كل صفحة. |
| 3 | **login_loading.html يثبّت السمة الداكنة بشكل ثابت** | `login_loading.html:2` | `data-theme="dark"` مثبت بشكل ثابت. إذا كان لدى المستخدم تفضيل السمة الفاتحة، ستظهر شاشة التحميل داكنة ثم يعاد التوجيه. صفحة التحميل تكرر نظام التصميم بالكامل (~100 سطر CSS) — كابوس صيانة. |
| 4 | **Service worker يخبّئ لكن لا يوجد بديل للتصفح دون اتصال** | `sw.js:19-31` | يخبّئ الملفات الثابتة فقط. إذا تصفح المستخدم دون اتصال، يحصل على صفحة الخطأ الافتراضية للمتصفح. يجب خدمة `offline.html` لطلبات التنقل. |
| 5 | **إعادة فتح الشريط الجانبي على سطح المكتب يكسر تجربة المستخدم** | `base.html:317-320` | عند تغيير حجم النافذة من الجوال إلى سطح المكتب، يُعاد فتح الشريط الجانبي تلقائياً حتى لو كان المستخدم قد أغلقه عمداً. لا توجد طريقة لطي الشريط للحصول على مساحة محتوى أكبر. |

---

## 🟡 عالية (8)

| # | المشكلة | الملف | التفاصيل |
|---|---------|-------|----------|
| 6 | **رموز تعبيرية (إيموجي) مستخدمة كأيقونات في النص** | `employee/dashboard.html:85`، `employee/leave.html:13` | استخدام `🟢` بشكل مباشر. تظهر بشكل مختلف عبر المتصفحات وأنظمة التشغيل. يجب استخدام مكتبة أيقونات أو عناصر CSS منسقة. |
| 7 | **رمز تعبيري في نص تذييل حساس** | `login.html:42` | `⚠️` في "هذه المنظومة مسجلة ومراقبة" — يبدو غير احترافي ويتعطل في بعض المتصفحات/الطرفيات. |
| 8 | **صفحة الجلسات تكرر عمود `device_type`** | `sessions.html:53,56` | عنوان العمود "الجهاز" و"نوع الجهاز" لكن كلاهما يعرض `s.device_type`. يجب أن يكون المتصفح ونوع الجهاز على التوالي. |
| 9 | **نماذج الحضور اليدوي — حقول CSRF مخفية زائدة عن الحاجة** | `manual_att.html:19,25` | كل بطاقة عسكري تحتوي على نموذجين مع إدخال CSRF مخفي. لـ 100 عسكري = 200 إدخال مخفي. يجب استخدام نموذج واحد لكل صف أو إرسال عبر JS. |
| 10 | **إحداثيات GPS ثابتة في القالب** | `attendance.html:58-60` | `facilityLat = 25.8633556`، `facilityLng = 43.4831471`، `allowedRadius = 50`. هذه إحداثيات وسط السعودية بشكل افتراضي. يجب أن تأتي من إعدادات الخادم أو متغير البيئة. |
| 11 | **لا يوجد ترقيم صفحات في أي جدول** | `users.html`، `sessions.html`، `security.html`، `audit_log.html`، `shifts.html` | جميع الجداول تعرض كل الصفوف. مع 500+ عسكري، هذا يقتل أداء الجوال وتجربة المستخدم. |
| 12 | **لا يوجد بحث/تصفية في جدول إدارة المستخدمين** | `users.html` | جدول المستخدمين يحتوي على الرتبة، الاسم، اليوزر، الجوال، الحالة، الجهاز لكن لا توجد طريقة لتصفية أو فرز. يجب على المستخدم التمرير عبر القائمة بأكملها. |
| 13 | **صفحة التقارير تفتقر إلى منتقي التاريخ** | `reports.html:48` | تعرض فقط التواريخ المؤرشفة كروابط. لا توجد طريقة لاختيار تاريخ عشوائي. يجب على المستخدم النقر عبر الأيام بالتسلسل. |

---

## 🟠 متوسطة (10)

| # | المشكلة | الملف | التفاصيل |
|---|---------|-------|----------|
| 14 | **متغير `accent-gold` مستخدم لكنه غير معرّف** | `circulars.html:45`، `employee/dashboard.html:107` | CSS يشير إلى `var(--gold)` في `.border-gold` و `var(--accent-gold)` في الشريط الجانبي — هذه المتغيرات غير معرفة في `:root`. لا يوجد بديل. |
| 15 | **وسم `<style>` داخل جسم القالب بدلاً من الرأس** | `circulars.html:45`، `reports.html:58-73` | أنماط CSS معرفة في منتصف الصفحة داخل وسوم `<style>`. أنماط الطباعة في التقارير موجودة في قالب `scripts`. يجب دمج CSS في `style.css`. |
| 16 | **حاوية الإشعارات المنبثقة موضوعة بأنماط JS مضمنة** | `app.js:19` | `style.cssText = 'position:fixed;top:20px;left:50%;...'` — يجب أن تكون كلاس CSS. أيضاً لا يوجد تكديس للإشعارات المتعددة — تتداخل فوق بعضها. |
| 17 | **تحميل Chart.js من CDN في كل زيارة للوحة التحكم** | `dashboard.html:137` | مكتبة 50KB+ تُحمّل من CDN بدون ضمان تخزين مؤقت. إذا كان CDN بطيئاً، تظهر منطقة الرسم البياني فارغة. يجب دمجها أو تخزينها مسبقاً في Service Worker. |
| 18 | **`color-mix()` غير مدعوم في المتصفحات القديمة** | `style.css:141,225,386` | مستخدمة لخلفيات الشريط العلوي والشريط الجانبي وبطاقة تسجيل الدخول.需要有 fallback للون ثابت لمتصفحات Safari <15، Firefox <113. |
| 19 | **Backdrop-filter بدون fallback** | `style.css:142-143,226-227,387-388` | تأثيرات الزجاج تفشل بصمت في المتصفحات التي لا تدعم `backdrop-filter`. لا يوجد لون خلفية ثابت بديل. |
| 20 | **لا توجد تسميات `sr-only` أو تسميات وصول للأزرار ذات الأيقونات فقط** | قوالب متعددة | أزرار التعديل، الحذف، الإيقاف في جدول المستخدمين تحتوي على خاصية `title` لكن لا توجد تسمية وصول لقارئات الشاشة. |
| 21 | **صفحة تسجيل الدخول لا تحتوي على زر إظهار كلمة المرور** | `login.html:34` | في السياق العسكري، غالباً ما يخطئ المستخدمون في كتابة كلمات المرور المعقدة. لا توجد طريقة لإظهار كلمة المرور قبل الإرسال. |
| 22 | **نافذة تأكيد الحذف تختار بمقارنة النص في DOM** | `users.html:143` | `inp.value.trim() !== lbl.textContent.trim()` — إذا كان اسم المستخدم يحتوي على علامات RTL/LTR أو تطبيع Unicode مختلف، تفشل المقارنة ويبقى الزر معطلاً. |
| 23 | **لا توجد حالة تحميل عند إرسال نموذج المستخدم** | `user_form.html` | نموذج إضافة/تعديل المستخدم لا يحتوي على spinner أو تعطيل عند الإرسال. إذا كانت الشبكة بطيئة، قد يؤدي الإرسال المزدوج إلى إنشاء مستخدمين مكررين. |

---

## 🔵 منخفضة (12)

| # | المشكلة | الملف | التفاصيل |
|---|---------|-------|----------|
| 24 | **رأس الجدول `table-dark` ثابت بدلاً من استخدام متغير CSS** | جداول متعددة | `<thead class="table-dark">` يشير إلى كلاس Bootstrap الداكن بدلاً من استخدام التنسيق المخصص لنظام التصميم. غير متناسق عند تغيير السمة. |
| 25 | **`btn-gold` مشار إليه لكنه غير معرّف** | `shifts.html:42`، `leaves.html:49`، `circulars.html:39` | كلاس CSS `btn-gold` مستخدم لكنه غير معرّف في `style.css`. يقع على تنسيق Bootstrap الافتراضي. |
| 26 | **شريط التقدم يعمل على كل نقرة رابط** | `base.html:258-268` | حتى الروابط الخارجية (التي تحتوي على `target="_blank"`) أو روابط التجزئة تحصل على شريط التقدم. قائمة الاستثناءات تعالج فقط `data-bs-toggle`، `#`، `javascript:`، و `download`. |
| 27 | **تحريك `.card:nth-child(n)` غير دقيق** | `style.css:493-495` | يعمل فقط لأول 3 بطاقات. إذا كانت الصفحة تحتوي على 8 بطاقات (لوحة التحكم)، البطاقات 3-8 لها نفس التأخير. يجب استخدام كلاس متدرج أو JS. |
| 28 | **أيقونة الحالة الفارغة تستخدم `d-block` المهمل على `<i>`** | `style.css:752` | `display: block` على عنصر أيقونة يجعل حاوية الأيقونة عنصر كتلة. الأفضل استخدام غلاف. |
| 29 | **خط الساعة يثبت `Courier New`** | `style.css:196` | الساعة الرقمية تستخدم `Courier New` الذي يظهر بشكل مختلف على الجوال مقابل سطح المكتب. يجب استخدام خط monospace مناسب للعربية أو خط النظام. |
| 30 | **لا يوجد خط خاص RTL للأرقام الأحادية المسافة** | `style.css:196` | الأرقام العربية (٠١٢٣) مقابل الأرقام الغربية (0123) تظهر بشكل مختلف. الساعة تظهر الأرقام الغربية — مقبول لكن يجب أن يكون مقصوداً. |
| 31 | **صفحة خطأ محدد السرعة قد تكون عدوانية جداً** | `app.py` | 300/دقيقة = 5 طلبات/ثانية. إذا كانت هناك علامات تبويب متعددة أو نقرات سريعة على تسجيل الحضور، قد يتم حظر المستخدم لمدة دقيقة بدون رسالة واضحة. |
| 32 | **لا يوجد وصف `meta` لتحسين محركات البحث/المشاركة الاجتماعية** | `base.html` | صفحة تسجيل الدخول لا تحتوي على وصف meta. مشاركات وسائل التواصل الاجتماعي ستظهر الرابط الخام. |
| 33 | **رسالة فشل تسجيل الدخول لا تفرق بين "المستخدم غير موجود" و"كلمة المرور خاطئة"** | `app.py` (مسار تسجيل الدخول) | أفضل ممارسة أمنية: يجب إعطاء نفس الرسالة العامة. حالياً قد يسبب تسريب لوجود المستخدمين. |
| 34 | **لا يوجد `loading="lazy"` على صور الأفاتار** | `profile.html:13`، `base.html:61`، `employee/dashboard.html:7` | صور الأفاتار يجب أن تستخدم التحميل البطيء الأصلي. |
| 35 | **`pageLoader` script يعمل في كل صفحة لكن CSS يقول `position: fixed`** | `style.css:90-95`، `base.html:25` | بعد `window.load`، المخفي يختبئ بـ `opacity: 0; visibility: hidden` لكنه لا يزال يشغل DOM. يجب إزالته بـ `display: none` بعد الانتقال. |

---

## 🎨 تدقيق نظام التصميم

| # | الملاحظة | التفاصيل |
|---|----------|----------|
| D1 | **لوحة الألوان محافظة، ليست عسكرية** | الأزرق يهيمن (أزرق، كحلي، سماوي). الأنظمة العسكرية عادةً تستخدم ألواناً أقل وأكثر هدفاً. 7 ألوان مميزة (أزرق، أخضر، أحمر، كهرماني، بنفسجي، سماوي، كحلي) تبدو وكأنها قالب لوحة تحكم عام. أوصي بتقليصها إلى 4: أزرق القيادة، أحمر التنبيه، أخضر الحالة، ذهبي التحذير. |
| D2 | **لا يوجد مقياس طباعي** | أحجام الخطوط عشوائية: `15px` للنص، `.82em` للصغير، `.75rem` لعناوين الإحصائيات، `1.1rem` لـ h2 في تسجيل الدخول. لا يوجد مقياس ثابت. |
| D3 | **الوضع الداكن داكن جداً** | خلفية `#0b1119` بحدود `#1e2d42`. في غرفة قيادة عسكرية ذات إضاءة خافتة، هذا التباين مقبول، لكنه يحتاج مساحة تنفس أكثر قليلاً. |
| D4 | **الوضع الفاتح ليس له شخصية** | الوضع الفاتح يعكس الألوان فقط — بطاقات بيضاء، خلفيات رمادية فاتحة. يفقد هوية الدرع العسكري تماماً. يجب أن يحمل الهوية الزرقاء/الذهبية. |
| D5 | **تأثير الزجاج مفرط الاستخدام** | الشريط العلوي، الشريط الجانبي، بطاقة تسجيل الدخول جميعها تستخدم `backdrop-filter: blur()`. هذا يخلق ثقلاً بصرياً وأداء ضعيفاً على الأجهزة القديمة. طبّق على عنصر واحد كحد أقصى لكل شاشة. |
| D6 | **الشريط الجانبي يبدو ضيقاً** | عرض 270px مع خط 14px، مساحة أفقية 10px داخل الروابط. النص قريب من الاقتطاع على الجوال. |
| D7 | **لا توجد أنماط `focus-visible`** | `:focus` منسق لكن `:focus-visible` غير مميز. مستخدمو لوحة المفاتيح يحصلون على نفس الحلقة كمستخدمي الماوس. |
| D8 | **تأخيرات التحريك عشوائية** | `.card:nth-child(1)` = 0ms، (2) = 50ms، (3) = 100ms. لا توجد صيغة أو نظام. |
| D9 | **لا يوجد تحسين طباعة لتقارير الحضور** | فقط `reports.html` يحتوي على أنماط طباعة (داخلية). الصفحات الأخرى لا تحتوي على أي تنسيق طباعة على الإطلاق. |

---

## 📊 الإحصائيات

| المقياس | القيمة |
|---------|--------|
| إجمالي أسطر CSS | 819 |
| إجمالي القوالب | 33 |
| إجمالي أسطر JS (جهة العميل) | ~250 |
| خصائص CSS المخصصة المعرفة | 48 |
| خصائص CSS المخصصة المستخدمة غير المعرفة | `--gold`، `--accent-gold` |
| الأنماط المضمنة في القوالب | ~15 مرة |
| الرموز التعبيرية المستخدمة كأيقونات | 3 مواقع |
| الصور بدون نص بديل | 3 على الأقل |
| كلاسات CSS غير المستخدمة | `section-header`، `table-success-row` |

---

## خطة العمل

نقترح معالجة هذه المشكلات في 5 مراحل:

1. **🔴 إصلاحات حرجة** (أيقونات manifest، رؤوس CSP، SW للتصفح دون اتصال، login_loading.html، سلوك الشريط الجانبي)
2. **🎨 إصلاح نظام التصميم** (دمج الألوان، مقياس طباعي، إصلاح الوضع الفاتح، إزالة الإفراط في تأثير الزجاج، إضافة رموز تصميم متوافقة مع RTL)
3. **🖼️ إعادة هيكلة القوالب** (إزالة الرموز التعبيرية، إصلاح الأعمدة المكررة، إضافة منتقي تاريخ، ترقيم صفحات، بحث/تصفية)
4. **⚡ تحسين الأداء وتجربة المستخدم** (تحميل بطيء، حالات تحميل للنماذج، نظام إشعارات منبثقة، تسميات وصول)
5. **🧹 تنظيف** (إزالة CSS غير المستخدمة، دمج الأنماط المضمنة، إصلاح عدم تطابق المتغيرات)
